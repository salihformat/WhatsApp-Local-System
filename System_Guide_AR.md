# دليل تشغيل واستخدام النظام المحلي (Local System)

هذا الدليل مخصص لشرح **النظام المحلي** بجميع تفاصيله التقنية والعملية، وكيفية تشغيله، بالإضافة إلى شرح مفصل لكل صفحة فيه، وكيفية التعامل مع مجلد المراقبة `C:\PrintMonitor`.

---

## الجزء الأول: تشغيل النظام المحلي والوصول إليه من أي جهاز

بما أنك تستخدم برنامج **XAMPP**، يمكنك إعداد النظام ليعمل بشكل دائم بمجرد تشغيل الكمبيوتر، والوصول إليه من أي جهاز آخر متصل معك على نفس شبكة الواي فاي، **بدون الحاجة لكتابة أمر `php artisan serve`**.

### الخطوة 1: معرفة عنوان الـ IP الخاص بجهازك
1. اضغط على زر `Windows` واكتب `cmd` لفتح موجه الأوامر.
2. اكتب الأمر `ipconfig` واضغط `Enter`.
3. ابحث عن السطر المكتوب فيه `IPv4 Address`. سيكون الرقم مشابهاً لـ `192.168.1.144` أو ما شابه (احفظ هذا الرقم).

### الخطوة 2: توجيه XAMPP لفتح النظام المحلي مباشرة
1. اذهب إلى المسار التالي في جهازك: `C:\xampp\apache\conf\extra`
2. افتح ملف `httpd-vhosts.conf` باستخدام أي محرر نصوص.
3. أضف الكود التالي في نهاية الملف (استبدل `192.168.1.144` برقمك الخاص):

```apache
<VirtualHost *:8001>
    DocumentRoot "C:/xampp/htdocs/whatsapp-local-system/public"
    ServerName 192.168.1.144
    <Directory "C:/xampp/htdocs/whatsapp-local-system/public">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```
4. احفظ الملف، ثم افتح لوحة تحكم XAMPP.
5. بجانب كلمة **Apache**، اضغط `Stop` ثم `Start` لإعادة تشغيله وتطبيق الإعدادات.

*(ملاحظة: يمكنك الآن الوصول للنظام عبر الرابط `http://192.168.1.144:8001` مباشرة).*

---

## الجزء الثاني: شرح صفحات ومميزات النظام المحلي بالتفصيل

النظام المحلي هو بمثابة "وسيط تشغيلي" خفيف، يتكون من ثلاث صفحات رئيسية:

### 1. صفحة لوحة القيادة (Dashboard) 
**الرابط:** `http://192.168.1.144:8001/dashboard`
هي واجهة التحكم والتشغيل الأساسية.
- **الإحصائيات والرسومات البيانية:** تعرض لك ملخصاً لعدد الرسائل (المرسلة، قيد الانتظار، التي يتم معالجتها، والفاشلة).
- **معلومات مجلد المراقبة:** تعرض حالة مجلد `C:\PrintMonitor` وما إذا كان متصلاً بنجاح.
- **الأزرار التشغيلية الهامة:**
  1. **فحص المجلد (Scan Folder):** عند الضغط عليه، سيقوم النظام فوراً بقراءة المجلد `C:\PrintMonitor` والبحث عن أي ملفات جديدة، ومن ثم سحبها وتجهيزها للإرسال.
  2. **معالجة قائمة الانتظار (Process Queue):** يقوم بدفع النظام لمعالجة وإرسال الرسائل المتوقفة في طابور الإرسال بشكل فوري.
  3. **إعادة إرسال الفاشلة (Retry All Failed):** يعيد جميع الرسائل الفاشلة إلى حالة "قيد الانتظار" لتبدأ محاولة الإرسال من جديد.

### 2. صفحة إدارة الرسائل (Messages)
**الرابط:** `http://192.168.1.144:8001/messages`
هنا تجد السجل الكامل لكل ما تم إرساله أو سحبه من المجلدات.
- **حالة المزامنة المباشرة (Live Sync):** النظام يقوم بالتحقق من حالة الرسالة مباشرة من السيرفر المركزي.
- **الإجراءات الجماعية (Bulk Actions):** يمكنك تحديد رسائل متعددة وحذفها أو إعادة إرسالها دفعة واحدة.
- **إعادة الإرسال الفردي (Retry):** زر لإعادة إرسال رسالة معينة فشلت في الوصول.

### 3. صفحة إدارة المستخدمين (Users)
**الرابط:** `http://192.168.1.144:8001/users`
*(هذه الصفحة تظهر فقط للمدير)*
- **الغرض:** إضافة مستخدمين أو موظفين جدد للنظام المحلي وتعيين كلمات مرورهم.

---

## الجزء الثالث: شرح آلية مجلد المراقبة الذكي (C:\PrintMonitor)

الميزة الأهم في هذا النظام المحلي هي قدرته على قراءة الملفات آلياً وإرسالها بمجرد حفظها في مجلد `C:\PrintMonitor`.

### أين يقع المجلد؟
يجب أن تتأكد من وجود مجلد باسم `PrintMonitor` داخل القرص `C:\`. إذا لم يكن موجوداً، سيقوم النظام بإنشائه تلقائياً.

### كيف تحفظ الملفات ليقوم النظام بإرسالها آلياً؟
النظام يعتمد على **"اسم الملف"** لمعرفة رقم الواتساب الذي يجب الإرسال إليه!

**القاعدة الذهبية:**
يجب أن يحتوي اسم الملف على **رقم الجوال** (من 9 إلى 15 رقماً متصلاً).
- **أمثلة صحيحة:**
  - `invoice_966501234567.pdf` (ممتاز، سيتم الإرسال لرقم 966501234567)
  - `966501234567.jpg` (صحيح)

**ماذا يفعل النظام عند حفظ الملف؟**
1. يقرأ الرقم من اسم الملف.
2. ينظف اسم الملف (يحذف رقم الجوال لكي يصل للمستلم باسم مثل `invoice.pdf`).
3. يضع الملف في طابور الواتساب للإرسال (Queue).
4. ينقل الملف الأصلي إلى مجلد `C:\PrintMonitor\processing`.

### ما هي صيغ الملفات المدعومة؟
`pdf`, `jpg`, `jpeg`, `png`, `doc`, `docx`, `xls`, `xlsx`, `csv`, `txt`

### أين تذهب الملفات إذا حدث خطأ؟
1. **مجلد `failed` (الفاشل):** يذهب إليه الملف إذا كان حجمه كبير جداً، أو لا يحتوي على رقم جوال، أو صيغته غير مسموحة.
2. **مجلد `archive` (الأرشيف):** توضع فيه ملفات الطباعة المؤقتة (`.bin`).
3. **مجلد `processing` (قيد المعالجة):** الملفات السليمة التي سحبها النظام بنجاح.

---

## الجزء الرابع: تشغيل العمليات في الخلفية (Queue & Scheduler) هام جداً!

لكي يعمل النظام بشكل "أوتوماتيكي بالكامل" (يسحب الملفات تلقائياً من C:\PrintMonitor ويرسلها للعملاء فوراً دون تدخل منك، ويعمل من جديد تلقائياً بعد كل إعادة تشغيل للجهاز أو انقطاع كهرباء)، **يجب تشغيل Apache + عامل الطابور + المجدول باستمرار**.

### الطريقة الموصى بها: إعداد تلقائي بنقرة واحدة (لا يحتاج تكراراً كل يوم)

بدل فتح نوافذ CMD يدوياً كل يوم، يوفّر المشروع سكربت إعداد جاهز يسجّل كل شيء ضمن **Windows Task Scheduler** (وApache كخدمة Windows حقيقية)، بحيث يعمل النظام تلقائياً من جديد بعد أي إعادة تشغيل للجهاز أو انقطاع كهرباء، ويعيد تشغيل نفسه تلقائياً إن تعطّل أي جزء منه، دون أي تدخل يدوي بعد ذلك أبداً:

1. تأكد من تثبيت XAMPP في `C:\xampp` (المسار الافتراضي) وأن المشروع موجود في `C:\xampp\htdocs\whatsapp-local-system`.
2. اذهب إلى مجلد `scripts` داخل مجلد المشروع.
3. انقر نقراً مزدوجاً على `Install-AutoStart.bat`.
4. اقبل طلب صلاحيات المسؤول (Administrator) الذي سيظهر — هذا مطلوب مرة واحدة فقط لتسجيل الخدمة والمهام.
5. انتظر حتى تظهر رسالة "تم الإعداد بنجاح!" ثم أغلق النافذة.

هذا كل ما يلزم. من الآن فصاعداً، Apache وعامل الطابور والمجدول ستعمل تلقائياً عند كل إقلاع للجهاز، وتُعيد تشغيل نفسها تلقائياً عند أي تعطل، دون فتح أي نافذة CMD يدوياً بعد اليوم.

> **ملاحظة:** بعد استخدام هذا السكربت، لا تستخدم أزرار "تشغيل/إيقاف/إعادة تشغيل الخدمات" في لوحة تحكم النظام، لأن العمليات أصبحت مُدارة بواسطة Windows Task Scheduler مباشرة، واستخدام الأزرار معه قد يُشغّل عاملاً مكرراً بالخطأ.
>
> لإزالة هذا الإعداد التلقائي لاحقاً (والعودة للتشغيل اليدوي)، نفّذ `Uninstall-AutoStart.bat` من نفس المجلد.

### الطريقة اليدوية (بديل، أو لأغراض الاختبار فقط)

إن كنت تفضّل عدم استخدام السكربت أعلاه، يمكنك تشغيل الأوامر يدوياً في كل مرة يُقلع فيها الجهاز:

1. افتح موجه الأوامر (CMD).
2. اكتب الأمر للتوجه لمجلد المشروع: `cd C:\xampp\htdocs\whatsapp-local-system`
3. اكتب الأمر `php artisan queue:work` واضغط Enter. ستلاحظ أن الشاشة أصبحت تراقب الطابور. (يجب ترك هذه الشاشة مفتوحة لتستمر عملية الإرسال الفورية).
4. افتح شاشة CMD أخرى (جديدة)، واكتب:
   `cd C:\xampp\htdocs\whatsapp-local-system`
   `php artisan schedule:work`
   (واترك هذه الشاشة مفتوحة لتقوم بالفحص التلقائي للمجلد كل دقيقة).

بديل سريع لهاتين الخطوتين: تشغيل `start-services.bat` الموجود في جذر المشروع (ينفّذ نفس الأمرين في نافذتين). لكن تذكّر أن هذه الطريقة اليدوية **لا تنجو من إعادة تشغيل الجهاز أو انقطاع الكهرباء** ولا تُعيد تشغيل نفسها تلقائياً عند التعطل — لذا يبقى سكربت الإعداد التلقائي أعلاه هو الخيار الموصى به لأي تشغيل فعلي لدى العميل.
